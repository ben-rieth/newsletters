HOST=localhost
PORT=8080
WEB_URL=http://localhost:3001
ENVIRONMENT="dev"
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/newsletters
JWT_SECRET=
RESEND_API_KEY=re_
NEWSLETTER_SENDER_EMAIL="newsletter@example.com"
JOB_QUEUE_SIZE=50
# Number of reverse proxies between the client and this server. Rate limiting
# walks back exactly this many X-Forwarded-For hops to find the client IP, so a
# wrong value either lets clients forge their IP (too high) or lumps everyone
# behind the proxy into one bucket (too low). 0 means no proxy.
TRUSTED_PROXY_COUNT=0